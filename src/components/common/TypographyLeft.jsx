import { Typography } from "@mui/material";
import { styled } from "@mui/material/styles";

export const TypographyLeft = styled(Typography)(() => ({
  textAlign: "left",
}));

export const TypographyLeftTitle = styled(Typography)(() => ({
  textAlign: "left",
  marginBottom: "24px",
  fontVariant: "small-caps",
}));
